{
  "name": "4d-frontend",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/xuchen81/4d-frontend.git",
  "author": "Petit Kero Pro <bigboss@hidoge.cn>",
  "license": "MIT",
  "scripts": {
    "deploy": "node deploy.js deploy 4d-frontend",
    "dev": "browser-sync start --server --files \"**/*.css, **/*.html\"",
    "clean": "rimraf dist",
    "build": "yarn clean && mkdir -p dist/assets && cp -r assets dist/ && cp *.html dist"
  },
  "dependencies": {
    "bulma-extensions": "^2.2.1",
    "font-awesome": "^4.7.0",
    "jquery": "^3.5.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "commander": "^2.20.0",
    "shelljs": "^0.8.3"
  }
}
